<div class="container">
        <div class="jumbotron">
        <h1 class="jumboheader">Post Your Comment</h1>
        <p><a class="btn btn-lg btn-success" href="/articles" role="button">Back to Saved Articles</a></p>
        
    </div>

  {{!--Display the scraped titles, links, and article snippets in this section.--}}
  <div class="row">
    <h1><a href="{{result.link}}" target="_blank">{{result.title}}</a></h1>
    <p>{{result.articleSnippet}}</p>
  </div>
  <div class="row">
    <h2>Posted Comments</h2>
  </div>
  {{!--If no comments exist then display the following ptag info--}}
  <div class="row">
    {{#unless result.comment}}
    <p>There is 0 comment.</p>
    {{/unless}}
  </div>
  {{!--Iterate through the comments by author and their message, display them, and have the option to delete them from the user view (also deletes from our db --}}
  <div class="row">
    <ul>
      {{#each result.comment}}
      <li style="list-style: none">
        <p>Comment: {{this.body}}</p>
        <button class="delete-button btn btn-secondary pull-right" data-comment="{{this._id}}">Delete Comment</button>
      </li>
      {{/each}}
    </ul>
    
  </div>
  {{!--Do a post method for the comment(s) a user adds and save it to the db--}}
  <form method="POST" action="/articles/{{result._id}}">
    <div class="form-group row">
      <label for="body">Message</label>
      <textarea class="form-control" id="body" name="body" rows="3" required></textarea>
    </div>
    <button class="btn btn-primary comment-article pull-right" type="submit">Submit</button>
  </form>

</div>